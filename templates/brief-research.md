<!--
A research brief (reading the project's own code, data or logs): research-<nnn>-<slug>.md. Its Web
questions go to a websearch worker first, and the launcher adds that worker's findings to this brief. Keep it to one question: a research worker
usually finishes in about 45 steps, and a broad survey should be split.
The launcher adds the standard report opening (Status, Verdict: answered | partly | not found, Summary, Left, Next).
-->
# Goal
<The one question to answer, as an outsider would ask it.>

# Context
- Where to start (files, functions, captures) and what is already known, so it doesn't re-derive it.
- The project map is added to every brief automatically; don't repeat it.

# Web questions
- <What the public web can answer first: library, engine or format behaviour, versions, known issues, how
  others solved it. Public wording only, nothing private from the project. Write "none" when there is none.>

# Scope
- Read only. Look in: <folders>. Not in: <folders that would waste steps>.

# Done when
- The question is answered with evidence, or the report says exactly what is still unknown.

# Report
- The answer, then each claim with `path:line`.
- What was verified vs inferred, and the cheapest check that could prove the answer wrong.
